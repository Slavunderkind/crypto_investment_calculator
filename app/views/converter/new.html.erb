<%= form_for :converter, url: converter_index_path do |c| %>
    <%= c.label :crypto_name, 'Convert crypto' %>
    <%= c.select :from, @crypto_names %>

    <%= c.label :amount, 'Amount' %>
    <%= c.text_field :amount, type: 'number', step: :any, min: '0', placeholder: 'Amount', required: 'required' %>

    <%= c.label :fiat_name, 'Convert in' %>
    <%= c.select :to, ['USD', 'EUR'] %>

    <%= c.submit 'Convert' %>
<% end %>